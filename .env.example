# Optional: Override the config file location
# PRINTDEX_CONFIG=/path/to/config.yaml

# Optional: Override the watch directory (backward compat, prefer config.yaml)
# WATCH_DIR=/path/to/your/3d-prints

# Optional: Server port (can also use --port or -p flag)
# PORT=3333
